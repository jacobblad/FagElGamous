@model FagElGamous.Models.Site

<!--After we finish the database, I'll fix the info here.  I'm just not sure which info we're showing...-->
<div class="p-1">
    <div class="card text-white bg-secondary">
        <div class="card-header">
            <h5 class="card-title">Book #@Model: @Model</h5>
            <h6 class="card-subtitle mb-2 ">By @Model @Model @Model</h6>
        </div>
        <div class="card-body">

            <p class="card-text"><strong class="font-weight-bold">Price:</strong> @Model</p>
            <p class="card-text"><strong class="font-weight-bold">Publisher:</strong> @Model.</p>
            <p class="card-text"><strong class="font-weight-bold">ISBN:</strong> @Model</p>
            <p class="card-text"><strong class="font-weight-bold">Category:</strong> @Model</p>
            <p class="card-text"><strong class="font-weight-bold">Genre:</strong> @Model</p>
            <p class="card-text"><strong class="font-weight-bold">Pages:</strong>  @Model</p>

        </div>
        @*<form id="@Model" asp-page="/Purchase" method="post">
            <input type="hidden" asp-for="BookId" />
            <input type="hidden" name="returnUrl" value="@ViewContext.HttpContext.Request.PathAndQuery()" />
            <span class="card-text p-1">
                @Model
                <button type="submit" class="btn-success btn-sm pull-right" style="float:right">Purchase</button>
            </span>
        </form>*@
    </div>
</div>